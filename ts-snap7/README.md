# node-snap7 (TypeScript, Async-Only)

`node-snap7` is an async-first TypeScript implementation of Siemens S7 communication protocols.

Current milestone:
- project scaffold
- strict TypeScript setup
- dual ESM/CJS build
- Vitest test harness
- public async-first API shape (`AsyncClient`)

Protocol implementation is added incrementally by task.
