export interface CodecModuleStatus {
  ready: boolean;
  note: string;
}

export const codecModuleStatus: CodecModuleStatus = {
  ready: false,
  note: "Codec primitives will be implemented in Task 2."
};
